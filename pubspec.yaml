name: magicoincompanion
description: Magi Coin Companion is app create to follow and track your Magi Coin acount, balance and miners. So you can have all the most relevant information in a same app.
publish_to: 'Rafgui012'
version: 0.1.3

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  http: ^1.1.2
  url_launcher: ^6.2.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon.png
